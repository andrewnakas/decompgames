# Five additional playable decomps

User confirmed September 21: strictly decompiled games with independent replacement assets. Released-source games, ordinary emulation, existing hosted games, asset prototypes, and alternate editions do not satisfy this milestone.

**Completed additions: 2 / 5.** Open Digger and Open Paths passed the strict release gate and production verification on September 22, 2026.

Each counted addition needs documented decompilation provenance, an open-source engine license, a complete independently authored asset package, exact source/build/checksum records, deployment, and muted launch/input/representative gameplay/save verification. Full-game content availability and full-playthrough verification remain separate claims. Audio remains unverified.

## Candidate findings

| Candidate | Evidence | Current decision |
| --- | --- | --- |
| Supaplex / OpenSupaplex | Pinned GPL decompilation builds to WASM; the Open Paths package contains only independently generated CC0 data | **Released as Open Paths; addition 2/5.** Its six-level navigation campaign has a null audio backend. Muted local and production tests passed title/menu input, player creation, movement, first-level completion, fresh-start progress, backup export, deletion, import, and restoration. |
| Prince of Persia / SDLPoP | https://github.com/NagyD/SDLPoP describes its DOS disassembly basis and GPL code | Eligible engine lead, not a ready addition. No complete independent graphics/levels/music pack established. Do not assume repository data shares the engine license. |
| Digger Remastered | https://github.com/sobomax/digger and https://www.digger.org/faq.html document remaster/source rights; its upstream WASM target builds at the pinned revision | **Released as Open Digger; addition 1/5.** Eight CC0 layouts plus independent graphics, font, title, and icon compile in a no-audio WASM variant. Static-data audit, a full level loop, death/restart, storage reload, exact-source packaging, local integration, and production play all pass; see `docs/digger-replacement-integration.md`. |
| ShortLine / reSL | https://github.com/konovalov-aleks/reSL documents a GPL-3.0 reverse-engineered game and upstream Emscripten target | Active addition 3 candidate, called Open Junction. The pinned code builds privately with independent CC0 external art, glyphs, cursor, scenario, vehicle alignment, and signal placement; the original resource directory is excluded. Muted Chromium passed Go input, player rail construction, a second station appearing, train dispatch, one successful train delivery, and Save/reload/Archive restoration. The actual shared player also passed export/delete/import and resumed a two-station game through Archive in a private preview. Trace-only tests entered the year-2000 transition and game-over branch; these do not prove continuous play or organic defeat. Wider multi-station gameplay, dynamic signal/redraw review, final provenance review, and publication remain open. The source appears to have repeating levels rather than a finite campaign; do not count it yet. |
| Tube (Bullfrog) / tube-game-dos | https://github.com/rep-stosw/tube-game-dos documents Hex-Rays/IDA source reconstruction under GPL-2.0 and an existing Emscripten build | Addition 4 lead, but the pinned upstream web recipe packages original `DATA/`, `SOUND/`, `DSEG3.bin`, and `DSEG4.bin`. The two extracted data segments alone total 626,176 bytes; `LoadDSEG()` loads both before wiring global pointers. At revision `d2c74cdbb82fedacc528f651ef28820bd186e0d2`, a replacement project must map those segments and each referenced asset format, build independently authored replacements, and verify the complete gameplay loop before hosting. The upstream browser binary is ineligible as-is. The related `tube64` is a variant of the same game, not a separate addition. |
| SkyRoads / skyroads-32x | https://github.com/haroldo-ok/skyroads-32x/tree/df219e03b854153291b32224593013b60d861d40 documents disassembly-based engine reconstruction, an MIT license, a Web build, and recovered data formats | Additional strict-decomp lead, not playable here. Upstream explicitly requires original SkyRoads data and distributes no replacement set. Its track generator splices one new road into a copy of the original `ROADS.LZS`; it does not create a complete independent game package. Full menu, ship, world, dashboard, object, level, and audio replacement or removal is required. The linked `ammaarreshi/skyroads-port` is the same engine lineage and cannot be counted separately. |
| SkiFree | https://github.com/yuv422/skifree_decomp reconstructs C code; https://github.com/jeff-1amstudios/skifree_sdl ports it | No clear engine open-source grant established in inspected material. Excluded from hosting pending evidence; replacement pictures alone would not resolve this. |
| Fire & Forget II | https://github.com/n0isy/fire-and-forget-2-recomp documents a browser recompilation that still requires extracted original graphics | Excluded from hosting: the inspected revision has no license grant for the reconstructed engine. Replacement graphics alone would not make the code open source. |
| Game Boy Tamagotchi / DanielBenGD | https://github.com/DanielBenGD/Gameboy-Tamagotchi-Decompilation documents a GPL-3.0 partial reconstruction | Not a near-term browser release. The repository contains analysis, hardware definitions, and 35 cleaned functions; its own README lists a compilable C build, SDL port, and WebAssembly as future work. It also describes ROM-extracted graphics rather than independently authored replacements. No playable build or clean asset pack was found in the inspected repository. |
| Forgotten Saga / LÖVE web port | https://github.com/NAMYUNWOO/forgottensaga_classic documents a browser port derived from function-by-function decompilation | The published 88 MB `game.data` contains original sprites, music, scripts, and maps according to the project's README, which reserves those asset rights to the original holders. No independent replacement campaign was established. Ineligible for this release gate despite an existing browser build. |
| Carmageddon / Dethrace web port | https://github.com/Carter54git/dethrace-ported-to-web documents a GPL-3.0 reverse-engineered engine and an Emscripten build | Its own deployment instructions require a roughly 200 MB `carmaweb.data` built from commercial game files and warn against publishing those assets. A complete independently authored replacement car, course, UI, and event set was not identified; the existing web package cannot count or be mirrored. |
| Twilight Princess / Dusk web port | https://github.com/sh1ftmaker/dusk-wasm documents an experimental WebGPU browser fork of the reverse-engineered Dusk project | Requires the visitor's original disc image and currently reaches setup screens while disc-streamed levels do not load. It has neither independent replacement game data nor a complete browser gameplay loop, so is not a near-term instant-play addition. |
| Pushover browser reconstruction | https://github.com/VorticonCmdr/pushover documents reverse engineering and a JavaScript simulation rebuilt from scratch | Its asset loader reads the original game's files; its editors could support newly authored levels and themes, but no complete independent package is established. It is an engine recreation rather than a documented source decompilation, so it cannot satisfy the strict five-game milestone. |
| arcade-js (Donkey Kong, The Pit, Frogger, others) | https://github.com/qarl/arcade-js documents GPL-3.0 instruction-by-instruction translation and subsequent readable decompilation of arcade ROM logic, plus a browser frontend | A serious future decomp lead, especially for smaller games, but the project explicitly still maps original ROM bytes as data and requires visitor-supplied graphics, palettes, and other tables. Before any instant-play release, independently author and validate a complete replacement ROM-data package for one game, while preserving its translated logic. Existing BYO-ROM play is ineligible for this milestone. |
| Space Invaders / arcade-js | The same pinned arcade-js revision has a complete idiomatic JavaScript runtime for its 8080 translation and a browser frontend; `games/invaders/manifest.js` declares only an 8,192-byte main CPU ROM, since the board draws a bitmap in RAM | Potentially smaller replacement-data task than Frogger, but the original CPU image still supplies sprites, text glyphs, work-RAM templates, and gameplay tables through `boards/invaders/memory.js`. The site cannot host its existing ROM-backed build. Map all runtime reads and author independent replacements before a private play test. |
| Zelda3 | Pinned engine and local-DAT validator exist | No independent full asset pack identified. Hold behind smaller candidates. |
| Bermuda Syndrome / Heart of Darkness | https://github.com/cyxx/bermuda and https://github.com/cyxx/hode describe engine reimplementations | Not accepted into the strict-decomp milestone without stronger provenance; no full replacement packs established. |
| LibreQuake / Blasphemer | https://github.com/lavenderdotpet/LibreQuake and https://github.com/Blasphemer/blasphemer offer free replacement content | Excluded from this milestone because their released-source engines do not meet the user's strict-decomp choice. |

This is a research queue, not five promised or verified releases. Continue searching smaller documented C/SDL decomps and recording license/data blockers. Do not invent an eligible fifth project to fill a table.

At inspected `arcade-js` revision `e849d086f4168c9a0e1ab501d62efbe3766def8a`, Frogger's manifest declares a 16,384-byte main CPU image, 4,096-byte graphics image, and 32-byte color PROM. The translated JavaScript executes game logic, but its memory system still reads original ROM bytes as data, and `Machine` decodes graphics from the supplied `gfx` and `proms` images. A replacement project would have to identify every live ROM-data address, supply independent tables plus new tile/sprite art and palette in the expected encoded formats, disable or replace the original audio samples, and test a complete level loop. The existing ROM-size and hash checks would need a separate honest manifest for that new data. This is a scoped candidate, not a ready browser release.

The same revision's Space Invaders board makes the tradeoff clearer. `boards/invaders/memory.js` returns the ROM image for every read through address `0x1fff`, even though the JavaScript translation runs the logic. Named ROM-backed resources in `games/invaders/idiomatic/names.js` include the alien sprite table at `0x1c00`, the 8×8 glyph bitmap table at `0x1e00`, score and firing-rate tables, and a work-RAM initialization image. A clean package would need new art and text in those encoded tables plus independently chosen balance and initialization data; simply zeroing the 8 KiB ROM would not preserve a playable game. Its lack of a separate graphics ROM makes it a useful candidate for a scoped replacement-data map, not an eligible release today.

## Open Paths release record

Open Paths uses OpenSupaplex revision `bad56a4e174e628643995284ea55d4c49af3137c` and immutable package `bad56a4e174e-026301f2dd`. The published corresponding-source archive includes the patched pinned source, generated browser patches, build record, replacement-data generators, planar codec, package manifest, and build instructions. The browser build uses the null audio backend and contains no upstream resource directory.

The replacement-only package contains 14 explicitly allowlisted files: generated fonts, fixed and moving graphics, palettes, screens, panel, and six levels. The first level is an intentionally short deterministic release-gate puzzle; the remaining five are generated perfect mazes. Muted production testing completed level one and verified saved completed-level progress and a two-file export/delete/import restoration. The complete six-level playthrough remains unverified and is stated on the game page.

Next: research and build eligible additions 3–5. No unsafe shortcut is implied by the five-game target.
