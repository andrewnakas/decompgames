import {readFile,access} from 'node:fs/promises';
import assert from 'node:assert/strict';
const games=JSON.parse(await readFile('src/data/games.json','utf8'));const ids=new Set();
for(const g of games){assert.match(g.id,/^[a-z0-9]+(?:-[a-z0-9]+)*$/);assert(!ids.has(g.id),`Duplicate ${g.id}`);ids.add(g.id);for(const key of ['title','description','overview','source','engineLicense','assetLicense','assetRequirements','saveInstructions'])assert(typeof g[key]==='string'&&g[key].length>0,`${g.id}: missing ${key}`);assert(new URL(g.source).protocol==='https:');assert(['instant','files','demo','project'].includes(g.mode));assert(Array.isArray(g.controls)&&Array.isArray(g.limitations));if(g.verification.status!=='unverified'){assert(g.verification.date&&g.verification.browsers.length&&g.verification.scope);}if(g.image)await access(`public${g.image}`);}
if(process.argv.includes('--release')){const playable=games.filter(g=>g.mode!=='project');assert(playable.every(g=>g.verification.status==='gameplay-tested'),'Every promoted integration must pass gameplay checks');assert(games.filter(g=>g.mode==='instant').length>=8,'Eight instant-play titles required');assert(games.filter(g=>g.mode==='files').length>=2,'Two local-import titles required');for(const g of playable){const m=JSON.parse(await readFile(`public/manifests/${g.id}.json`,'utf8'));assert(m.sourceRevision&&m.files?.length);await access(`public${m.sourceArchive}`);}}
console.log(`Validated ${games.length} catalog entries${process.argv.includes('--release')?' and release gates':''}.`);
